{
  "name": "collectible-cards-shop",
  "private": true,
  "workspaces": [
    "frontend/web"
  ],
  "scripts": {
    "start": "npm --workspace frontend/web run start",
    "build": "npm --workspace frontend/web run build",
    "test": "npm --workspace frontend/web run test",
    "lint": "npm --workspace frontend/web run lint"
  }
}
